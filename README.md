# Learning Repo

A mono-repo containing both:
- `learning-web` — React + Vite + Chakra UI Web App
- `learning-app` — React Native app using WebView to open hosted web app

## 📦 Installation

```bash
npm run setup